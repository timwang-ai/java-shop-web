<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bgmfeng.my.shop.web.admin.dao.TbUserDao">

    <sql id="tbUserColumns">
        a.id,
        a.username as a.userName,
        a.password,
        a.phone,
        a.email,
        a.created,
        a.updated
    </sql>
    <select id="selectAll" resultType="TbUser">
        SELECT
          <include refid="tbUserColumns"/>
        FROM
          tb_user AS a
    </select>
    <insert id="insert">
    INSERT INTO user (
        `username`,
        `password`
    )
    VALUES(
        #{username},
        #{password}
      )
    </insert>
    <delete id="delete">
        DELETE FROM tb_user WHERE id = #{id}
    </delete>
    
    <select id="getById" resultType="TbUser">
        SELECT
          <include refid="tbUserColumns"/>
        FROM
          tb_user AS a
        WHERE
          a.id = #{id}
    </select>
    <update id="update">
        UPDATE
          users
        SET
          username = #{username},
          password = #{password}
        WHERE id = #{id};
    </update>
    <select id="selectByUsername" resultType="TbUser">
        SELECT
          a.id,
          a.username as a.userName,
          a.password,
          a.phone,
          a.email,
          a.created,
          a.updated
        FROM
          tb_user AS a
        WHERE
          a.username LIKE CONCAT('%', #{username}, '%')
    </select>
</mapper>